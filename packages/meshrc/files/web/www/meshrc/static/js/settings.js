// where to find the ubus daeom
ubus_url = "/ubus"

// properties to show in graph and overview table
// must exists with same name in in the NetJson output generated by `p2n`
// also add formater function if desired
properties_active = {
    "load": ["Load", false],
    "memory": ["Free memory", fmt_percent],
    "traffic_mesh": ["Traffic Mesh", fmt_filesize],
    "traffic_ap": ["Traffic AP", fmt_filesize],
    "clients": ["Clients", fmt_default],
    "model": ["Device", fmt_default],
    "uptime": ["Uptime", fmt_duration]
}

// counter for ubus calls - shouldn't be changed
ubus_counter = 1

