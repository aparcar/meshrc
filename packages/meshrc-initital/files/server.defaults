#!/bin/sh

[[ -d /www/config ]] || mkdir /www/config

uci set uhttpd.nodeconfig=uhttpd
uci set uhttpd.nodeconfig.home='/www/config'
uci set uhttpd.nodeconfig.listen_http='8123'
uci commit uhttpd

/etc/init.d/uhttpd restart
